fastapi==0.110.0
uvicorn[standard]==0.29.0
httpx==0.27.0
gradio_client==1.4.0
python-multipart==0.0.9
numpy==1.26.4
# Optional: Only needed if using local PyTorch model (predict.py)
# Uncomment if you want to use local inference instead of Hugging Face
# torch==2.1.2
# torchvision==0.16.2
# Pillow==10.2.0
